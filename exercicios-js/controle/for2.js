const notas = [6.7, 7.4, 9.8, 8.1, 7.7]

for (let i in notas) {
    console.log(1, notas[1])
}

const pessoa = {
    nome: 'Flavia',
    sobrenome: 'Regina',
    idade: 35,
    peso: 54
}

for (let atributo in pessoa) {
    console.log(`${atributo} = ${pessoa[atributo]}`)
}